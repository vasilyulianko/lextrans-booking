# lextrans-booking


## Development
### Backend start

`./gradlew bootRun`

### Frontend start

`cd web`

`npm install`

`npm start`

## Deployment to heroku

`git push heroku master`

or

`git push origin master` (in  case heroku builds from GitHub)